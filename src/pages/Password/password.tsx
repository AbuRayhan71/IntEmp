import React from 'react';
import PasswordCard from './password'; // Update the import path based on your file structure

const Login: React.FC = () => {
  return (
    <div>
      <PasswordCard /> {/* Render the SimpleCard component here */}
    </div>
  );
}

export default Login;
