import React from 'react';
import Resetpassword from './Resetpassword'; // Update the import path based on your file structure

const Login: React.FC = () => {
  return (
    <div>
      <Resetpassword /> {/* Render the SimpleCard component here */}
    </div>
  );
}

export default Login;
