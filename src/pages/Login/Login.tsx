import React from 'react';
import SimpleCard from './Login'; // Update the import path based on your file structure

const Login: React.FC = () => {
  return (
    <div>
      <SimpleCard /> {/* Render the SimpleCard component here */}
    </div>
  );
}

export default Login;
